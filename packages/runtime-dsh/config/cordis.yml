# whalepod Runtime profile root — an empty entry list.
#
# The tree is composed entirely of patch layers (same contract as a dsh
# profile root): `@deepseek-ai/dsh-base`'s bundle patch first, then
# `whalepod.patch.yml` (the whalepod runtime bundle), then test-only
# overlays such as `replay.yml` (Q3 contract probes). Boot code lives in
# `packages/runtime-dsh/src/bridge.ts`; do not add rows here.
[]
