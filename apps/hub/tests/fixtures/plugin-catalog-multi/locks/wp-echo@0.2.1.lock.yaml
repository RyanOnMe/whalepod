# wp hub 集成测试 fixture：受审依赖闭包（空闭包）。同名多版本 fixture（M9 用）。
# dependencyLockDigest 算法见 ../plugin-catalog/locks/wp-fixed-time@0.1.0.lock.yaml 头注。
schemaVersion: 1
package:
  name: wp-echo
  version: 0.2.1
dependencies: []
